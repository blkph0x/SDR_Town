This document provides a comprehensive audit of your SDR and audio codebase. It profiles structural memory bugs, thread synchronization gaps, performance bottlenecks, and the explicit structural chain required to transition your raw I/Q baseband pipeline into clear, continuous P25 Phase 1 and Phase 2 digital voice audio.Part 1: High-Severity Structural Bugs & Concurrency Gaps1. Real-Time Memory Layout Corruption (AudioEngine.cpp)The Bug: In AudioEngine::startDevice(), the device configuration pointer is set using cfg.pUserData = this; (passing the outer AudioEngine object address). However, the real-time audio thread callback data_callback executes the following cast immediately:C++auto* myAct = reinterpret_cast<AudioEngine::ActiveOutput*>(pDevice->pUserData);
The Impact: Because AudioEngine and ActiveOutput have completely mismatched memory alignments and properties, evaluating atomic flags on myAct or parsing myAct->owningEngine triggers undefined behavior, corrupted variable lookups, or an uncatchable segmentation fault.The Production Requirement: Point cfg.pUserData explicitly to the targeted instance of ActiveOutput when instantiating the device.2. Teardown Race Condition & Object Lifetime Collapse (AudioEngine.cpp)The Bug: The class destructor acquires a scoped lock and immediately calls miniaudio teardown functions:C++std::lock_guard<std::mutex> lk(audioMutex);
// ... loops and calls ma_device_stop / ma_device_uninit ...
m_active.clear();
The Impact: ma_device_uninit stops playback, but background hardware worker threads may still be processing data or cleaning internal handles. Dropping structural containers immediately via m_active.clear() while background callbacks are finishing strips memory references away from underneath active thread frames, creating immediate use-after-free conditions.The Production Requirement: Transition to a multi-stage teardown. First, mark states as invalid. Second, exit the mutex context and call hardware join routines (ma_device_stop, ma_device_uninit) to ensure background worker execution scopes complete cleanly. Finally, secure the lock once more and clear the smart pointer vectors.3. Real-Time Lock-Free Bitwise Indexing Vulnerability (AudioEngine.cpp)The Bug: The ring buffer uses a fast masking wrap for circular navigation: (rpos + i) & (rb.capacity - 1).The Impact: This bitwise optimization is only mathematically functional if rb.capacity is guaranteed to be a precise power of two ($2^n$). If dynamic runtime adjustments initialize rb.capacity to an arbitrary integer boundary, the masking computation wraps read/write offsets to invalid memory addresses.The Production Requirement: Inject standard static_assert verifications or runtime initialization guards confirming (rb.capacity & (rb.capacity - 1)) == 0.4. Multithreaded Cache Line False Sharing (AudioEngine.cpp)The Bug: In the ActiveOutput metadata structures, the read pointer tracker (ring.readPos) and the write pointer tracker (ring.writePos) occupy contiguous physical space in memory.The Impact: The real-time miniaudio hardware thread continuously mutates readPos while your background DSP or SDR stream thread writes to writePos. Because modern CPUs cache memory in 64-byte blocks ("cache lines"), both CPU cores repeatedly invalidate each other's cache lines. This creates hidden performance degradation, thread stuttering, and audio dropouts.The Production Requirement: Insert standard hardware alignment qualifiers to enforce physical cache separation between thread boundaries:C++alignas(64) std::atomic<size_t> writePos{0};
alignas(64) std::atomic<size_t> readPos{0};
Part 2: DSP Bottlenecks & Architectural Shortfalls1. Quadratic $O(N \log N)$ Sorting on the Real-Time Stream Line (Demod.cpp)The Bug: Internal percentile, threshold, and noise-floor computations rely on an identical helper configuration:C++static double localPercentile(std::vector<double> values, double percentile, double fallback) {
    // ...
    std::sort(values.begin(), values.end());
    return values[idx];
}
The Impact: Extracting baseline noise-floor metrics over large arrays (such as 4096-bin FFT vectors) dozens of times per second scales poorly under full sorting workloads. Running a full $O(N \log N)$ quicksort iteration just to extract a single indexed threshold point consumes unnecessary CPU cycles.The Production Requirement: Swap out std::sort for std::nth_element. This utilizes an introselect algorithm that partitions elements in linear time ($O(N)$ average complexity), bypassing full array sorting entirely.2. Transformed Math Bottlenecks via std::pow (Demod.cpp)The Bug: Centroid frequency weighting and signal classification matrices continuously convert spectral decibels to linear energy inside tight loops using std::pow(10.0, db / 10.0).The Impact: Transformed floating-point exponent equations are slow when processed linearly across dense arrays, and standard compilers cannot reliably auto-vectorize them via SIMD assembly pipelines (such as AVX2).The Production Requirement: Build a low-overhead static Look-Up Table (LUT) covering your bounded dB domain (e.g., a fixed array mapped from $0.0$ to $70.0$ dB in 0.1 dB steps) to replace real-time exponential math calls with linear index lookups.3. Real-Time Allocation Violations (Demod.cpp & P25Control.cpp)The Bug: Functions inside processing paths copy vectors by value (e.g., std::vector<double> values in localPercentile) or dynamically allocate short frame arrays (e.g., std::vector<uint8_t> out(12, 0) in normalizeTsbkBlock).The Impact: Heap allocations (malloc/free) depend on internal kernel memory locks. If a high-priority real-time audio thread blocks waiting for a low-priority user-interface thread to release a heap lock, it causes a priority inversion, creating an immediate audio glitch or stream buffer overrun.The Production Requirement: Pass persistent scratch buffers by reference to eliminate dynamic reallocation routines, and replace small, bounded-size arrays with stack-allocated std::array configurations.Part 3: Engineering Requirements for a Continuous P25 ChainThe provided bit mapping functions are only the final layer of an RF receiver. To transform raw I/Q samples into clear, stable voice audio, your development team must implement the following multi-stage processing architecture:[ Raw Complex I/Q Samples ] 
             |
             v
 [ Digital Down Converter ] ---> Channel Filters & Decimation (12.5 kHz Channel Isolation)
             |
             v
 [ Baseband Demodulator ]   ---> P1: FM Discriminator | P2: Complex Phase Shift Tracking
             |
             v
[ Symbol Synchronization ]  ---> 2nd-Order Gardner Loop / Interpolator (Timing Recovery)
             |
             v
 [ Symbol-to-Bit Mapping ]  ---> P1: 4-Level C4FM Dibits | P2: H-DQPSK Phase Transitions
             |
             v
 [ Frame Sync Correlator ]  ---> Cross-Correlation Shift Register (Pattern Sync Matching)
             |
             v
[ De-interleaver & FEC ]    ---> Reed-Solomon / Golay Matrix Math (Error Correction)
             |
             v
 [ Voice Codec Pipeline ]   ---> Bit Unpacking and Struct Formatting for 'mbelib'
             |
             v
  [ Polyphase Resampler ]   ---> Interpolation Filter Stage (8000 Hz Speech -> Host Audio Device Rate)
Step 1: Channelization and Fine FilteringThe Requirement: Raw I/Q streams from a radio peripheral span wide sample bandwidths (e.g., 2.4 MHz). The architecture must deploy a Digital Down Converter (DDC) composed of a complex mixer, low-pass filter, and a decimation stage to isolate a single 12.5 kHz P25 channel.Phase 1 Target: Output standard baseband signal samples to a digital FM discriminator loop.Phase 2 Target: Preserve complex I/Q tracking vectors directly for multi-phase modulation parsing.Step 2: Symbol Timing Synchronization (The Missing Core Link)The Requirement: Hardware sample clocks will naturally drift away from the transmitter's symbol clock. Processing baseband data at fixed sample steps will eventually corrupt the decoder's synchronization.Implementation: Your team must inject a Second-Order Loop Filter combined with a Gardner Timing Error Detector (TED) and a fractional polynomial interpolator. This system tracks symbol timing variations dynamically, adjusting sample windows to extract a single stable data point from the precise center of every symbol.Phase 1 Input Rate: 4,800 symbols per second.Phase 2 Input Rate: 6,000 symbols per second.Step 3: Precise Synchronization Frame CorrelationThe Requirement: Bits extracted by the symbol clock are unstructured until aligned to frame boundaries. The team must build a sliding bit-shift tracking register to isolate the explicit network synchronization tokens.Phase 1 Target: Correlate against the standard 48-bit voice synchronization marker (0x5575F5FF77FFULL).Phase 2 Target: Track outbound TDMA slot timing structures using the explicit Phase 2 H-DQPSK synchronization pattern. This component must handle an allowed error tolerance (typically 2 to 3 bit mismatch thresholds) to maintain audio continuity when handling weak radio frequency signals.Step 4: De-interleaving and Forward Error Correction (FEC)The Requirement: Radio channels introduce noise bursts that corrupt contiguous blocks of data. P25 streams protect voice payloads by interleaving bits across a distributed data layout.Implementation: The code must route the synchronized bit arrays through a reciprocal de-interleaver matrix, reassembling them into functional blocks. These blocks are then passed through parity-check math architectures—specifically Golay (23, 12) and Reed-Solomon (36, 20) decoding engines—to compute error correction vectors before payload extraction.Step 5: Interface-Compliant Bit Unpacking for mbelibThe Requirement: Your existing code references mbe_processAmbe3600x2450Framef(), but passing raw bytes into this function directly will cause catastrophic synthesis corruption.Implementation: mbelib does not parse packed bit structures. Your team must construct an explicit conversion engine that unpacks individual data bit values into arrays of distinct character flags (0 or 1).Phase 1 Format: Map the extracted voice frame payload into a distinct four-line, 24-bit matrix pointer topology (char ambeMatrix[4][24]).Phase 2 Format: Correctly handle the two alternating TDMA slots, dropping frames from non-targeted slots while extracting the raw voice payloads from the targeted communication channel.Step 6: Audio Resampling & Native Playback IntegrationThe Requirement: The AMBE voice decoder produces raw voice audio at a fixed, hard-coded sample rate of exactly 8,000 Hz. Pushing an 8 kHz audio buffer straight to modern hardware outputs configured for native sample streams (like 44.1 kHz or 48 kHz) will playback at incorrect speeds or fail completely.Implementation: The dev team must integrate a polyphase linear or sinc interpolation filter. This resampler buffers the 8,000 Hz synthesis frames, interpolates intermediate values, and steps up the sampling rate to match the native playback target of the host system's hardware audio device. This upsampled stream is what safely feeds into your lock-free ring buffers.