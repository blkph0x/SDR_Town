[package]
name = "sdrtown_sstv"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
publish = false

[dependencies]
sstv = { path = "vendor/sstv", default-features = false, features = ["std"] }

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
strip = true
